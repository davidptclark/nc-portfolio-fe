import { View, Text, StyleSheet } from "react-native";

export default Home = () => {
  return (
    <View style={homeContainer}>
      {/* 
    <Header />
    <SortBar />
    <VideoFeed />  
  */}
    </View>
  );
};

const styles = StyleSheet.create({
  homeContainer: {},
});
